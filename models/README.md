# Gesture Recognizer Model

Place the MediaPipe model here:

```text
models/gesture_recognizer.task
```

Download with:

```powershell
python scripts/download_gesture_model.py
```

The service expects this local file so the demo does not depend on runtime network downloads.
